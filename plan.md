# Implementation plan

## Bead map

_Generated by gastown planning sync (open bead IDs from `bd list`). Expand acceptance bullets if QA requires more detail._

### te-8ps: linkshelf/go.mod
- Scope: Implement `linkshelf/go.mod` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `linkshelf/go.mod` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-8ps` is closed only after verification passes.

### te-8cz: linkshelf/internal/store/schema.go
- Scope: Implement `linkshelf/internal/store/schema.go` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `linkshelf/internal/store/schema.go` under the rig worktree.
  - Exports `InitSchema(*sql.DB) error` (or equivalent) with `CREATE TABLE IF NOT EXISTS` DDL from architecture.md.
  - DDL matches architecture.md; production entrypoint and package tests call this helper — no duplicated `CREATE TABLE` strings in tests or main.
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead is implemented before `internal/store/store.go` in build order.
  - Bead `te-8cz` is closed only after verification passes.

### te-uam: linkshelf/internal/store/store.go
- Scope: Implement `linkshelf/internal/store/store.go` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `linkshelf/internal/store/store.go` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Package has unit tests in `linkshelf/internal/store/store_test.go` (same bead or dedicated test bead) covering SPEC behavior before close.
  - `go test` for this package must pass (Verify runs `go test -count=1 ./<pkg>/...`).
  - Store methods match SPEC (`List`/`Create`/`Delete` with `context.Context`); tests use `:memory:` + `InitSchema` — not a shared `./links.db`.
  - Bead `te-uam` is closed only after verification passes.

### te-thd: linkshelf/internal/store/store_test.go
- Scope: Implement `linkshelf/internal/store/store_test.go` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - Unit tests assert **functional requirements** from SPEC.md and this plan section (happy path, errors, edge cases named in architecture).
  - No trivial always-pass tests (`assert True`, empty `TestX` bodies, or tests that only import the module).
  - Test names and cases map to SPEC/plan acceptance bullets (reference FR IDs or behavior in comments when helpful).
  - File exists at `linkshelf/internal/store/store_test.go` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-thd` is closed only after verification passes.

### te-tua: linkshelf/internal/api/handlers.go
- Scope: Implement `linkshelf/internal/api/handlers.go` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `linkshelf/internal/api/handlers.go` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Package has unit tests in `linkshelf/internal/api/handlers_test.go` (same bead or dedicated test bead) covering SPEC behavior before close.
  - `go test` for this package must pass (Verify runs `go test -count=1 ./<pkg>/...`).
  - Bead `te-tua` is closed only after verification passes.

### te-rnd: linkshelf/internal/api/handlers_test.go
- Scope: Implement `linkshelf/internal/api/handlers_test.go` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - Unit tests assert **functional requirements** from SPEC.md and this plan section (happy path, errors, edge cases named in architecture).
  - No trivial always-pass tests (`assert True`, empty `TestX` bodies, or tests that only import the module).
  - Test names and cases map to SPEC/plan acceptance bullets (reference FR IDs or behavior in comments when helpful).
  - File exists at `linkshelf/internal/api/handlers_test.go` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-rnd` is closed only after verification passes.

### te-2vk: linkshelf/web/app.js
- Scope: Implement `linkshelf/web/app.js` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `linkshelf/web/app.js` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-2vk` is closed only after verification passes.

### te-ga2: linkshelf/web/index.html
- Scope: Implement `linkshelf/web/index.html` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `linkshelf/web/index.html` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-ga2` is closed only after verification passes.

### te-85a: linkshelf/web/style.css
- Scope: Implement `linkshelf/web/style.css` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `linkshelf/web/style.css` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Bead `te-85a` is closed only after verification passes.

### te-avv: linkshelf/cmd/server/main.go
- Scope: Implement `linkshelf/cmd/server/main.go` per `architecture.md` and SPEC.md.
- Architecture: see architecture.md (paths and data model for this file).
- Acceptance:
  - File exists at `linkshelf/cmd/server/main.go` under the rig worktree.
  - Matches layout and naming in architecture.md (no stub/placeholder implementation).
  - Polecat runs **Verify** from the Next bead line before `bd close`.
  - Package has unit tests in `linkshelf/cmd/server/main_test.go` (same bead or dedicated test bead) covering SPEC behavior before close.
  - `go test` for this package must pass (Verify runs `go test -count=1 ./<pkg>/...`).
  - Bead `te-avv` is closed only after verification passes.

